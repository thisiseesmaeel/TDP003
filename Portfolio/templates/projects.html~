{% extends "base.html" %}
<meta name="viewport" content="width=device-width, initial-scale=1.0">
{% block title %} Projects page {% endblock %}
{% block head %}
{% endblock %}
{% block body %}
<h1> Projects </h1>
<p>

<form class="" action="/list" method="GET">
  <input type="text" name="search" placeholder="Search in projects">
  <input type="submit" name="search_button" value="Search">
  
  <select name="sort_order">
    <option value="desc">Descending</option>
    <option value="asc">Ascending</option>
  </select>
  
  <select name="sort_by">
    <option value="start_date">Start Date</option>
    <option value="project_name">Project Name</option>
    <option value="end_date">End Date</option>
  </select>
  <br>
  <br>
  
  <label for="fields"><input type="checkbox" name="search_fields">Project name</label>
  <label for="fields"><input type="checkbox" name="search_fields">Long Description</label>
  <label for="fields"><input type="checkbox" name="search_fields">Short Description</label>
  <label for="fields"><input type="checkbox" name="search_fields">Start date</label>
  <label for="fields"><input type="checkbox" name="search_fields">End Date</label>
  <label for="fields"><input type="checkbox" name="search_fields">Group Size</label>
  <label for="fields"><input type="checkbox" name="search_fields">Course name</label>
  <label for="fields"><input type="checkbox" name="search_fields">Course ID</label>
  <label for="fields"><input type="checkbox" name="search_fields">Academic Credits</label>
</form>

</p>

{% for project in db %}
<a href="/project/{{ project.get('project_id') }}">      <a href="{{url_for('project_site', project_id=project['project_id']) }}"><img src="../static/images/placeholder.png" alt="Project" width="250" height="250""><p>{{project.project_name}}</p></a>
{% endfor %}

{% endblock %}    

